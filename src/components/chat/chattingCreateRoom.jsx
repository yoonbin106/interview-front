import React, { useRef, useEffect } from 'react';

const ChattingCreateRoom = () => {
    const listsEndRef = useRef(null);

    // useEffect(() => {
    //     listsEndRef.current?.scrollIntoView({ behavior: "smooth" });
    // }, [lists]);

    return (
        <div className={styles.chattingCreateRoomContainer}>
                        
            





        </div>
    );
};

export default ChattingCreateRoom;